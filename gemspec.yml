name: ffi-libnet
summary: Ruby FFI bindings for libnet.
description: Ruby FFI bindings for libnet.
license: MIT
authors: Postmodern
email: postmodern.mod3@gmail.com
homepage: http://github.com/sophsec/ffi-libnet
has_yard: true

requirements: libnet >= 1.0.0

dependencies:
  ffi: ~> 1.0

development_dependencies:
  rubygems-tasks: ~> 0.1
  rspec: ~> 2.0
  yard: ~> 0.7
